<div class="container mt-3 p-3 text-center border">
  <h1>Test</h1>
</div>

<div class="planet_grid p-3 text-center border">
  <% @planets.each do |planet| %>
    <div class="mt-3">
      <%= planet.name %>
      <br><%= planet.details%>
      <br><%= planet.location %>
      <br><%= planet.price_per_night %>
    </div>
  <% end %>
</div>
